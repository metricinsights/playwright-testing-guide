# ================================================================
# Metric Insights API Test Configuration
# ================================================================
# Copy to: .env.<instance-name> (e.g., .env.dev, .env.stg70)
# Usage: npm run test:dev <instance> [test-file]
# Examples:
#   npm run test:dev stg70
#   npm run test:dev stg70 auth-admin.spec.ts
# ================================================================

# Instance Configuration
BASE_URL="https://your-instance.metricinsights.com"

# API Credentials (Admin > System > External Applications)
APPLICATION_ID="your_application_id"
APPLICATION_KEY="your_application_key"

# Admin User
USERNAME_ADMIN=
DEFAULT_USERNAME_ADMIN=
DEFAULT_PASSWORD_ADMIN=
USER_PASSWORD=

# Test Users (auto-created if missing)
USERNAME_POWER="playwright_power_user"
USERNAME_REGULAR="playwright_regular_user"
POWER_ID=""
REGULAR_ID=""

# Email Configuration
POWER_EMAIL="power.user@example.com"
REGULAR_EMAIL="regular.user@example.com"
AUTOTEST_EMAIL="autotest@example.com"

# ================================================================
# Quick Start:
# 1. Copy this file: cp .env.example .env.<instance>
# 2. Fill in your credentials
# 3. Run tests: npm run test:dev <instance> [test-file]
# ================================================================
